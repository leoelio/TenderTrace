# TenderTrace 项目开发工作流与阶段计划

基于赛题说明与《标迹TenderTrace技术架构与实现文档》v1.0。计划基准日期：2026-07-04，时区：Asia/Shanghai。

## 1. 我对任务的理解

本项目不是单纯做一个爬虫，而是做一个可运行、可验证、可持续订阅的招投标情报聚合系统。用户用自然语言描述需求，系统需要解析主题、区域、时间范围和发送频率；没有频率就立即执行，有频率就创建定时任务；每轮执行从至少两个真实互联网来源抓取招投标信息，其中至少一个来源需要登录后才能获取免费会员可见内容；系统完成清洗、筛选、去重、摘要、事实校验后生成 Word 报告。

最终必须交付四类成果：工具执行后的多个 Word 报告、设计文档、完整代码与操作文档、Demo 演示视频。报告必须包含标题、发布时间、来源链接、核心内容、附件链接；订阅任务必须只推送新增内容；程序不得对结果硬编码。

## 2. 关键假设与待确认点

为避免在开发中默默猜测，我先把会影响实现的解释写清楚：

- “发送给我”的默认交付先实现为 Web UI 下载 + 本地 outbox 归档；SMTP 邮件作为可配置通道。如果你明确要求企业微信、邮箱或其他渠道，后续在订阅模块追加。
- “最近 N 个月”默认解释为以触发时刻为终点的滚动窗口，并在意图确认卡中展示求值后的具体日期，允许用户修正。
- “今天 9:00 发送”如果当前时间已超过 9:00，则立即生成并在 UI 与报告中标注“指定时刻已过，已即时生成”。
- 登录站采用人工登录一次、加密保存 Playwright storage_state 的方式；系统不做验证码识别或绕过。
- LLM 是增强项而不是硬依赖。核心链路必须在无 LLM API 时以规则与确定性算法降级运行；有 LLM 时用于主题泛化、适配器编译和受限润色。
- 数据来源以中国政府采购网、省级/全国公共资源交易平台、千里马/采招网/剑鱼标讯等免费可见范围为候选。若某登录站免费权限不足，按统一适配器契约切换备选站。

## 3. 总体成功标准

项目完成时应达到以下表现：

- 输入赛题四类示例问题和评委现场变化问题，系统能解析出正确的主题、区域、时间范围与调度语义。
- 至少两个真实来源进入报告，其中至少一个来源通过登录态获取免费会员可见信息。
- 报告文件名符合 `{用户的问题}_{yyyyMMddHHmm}.docx`，非法字符、超长问题、同分钟重复生成都有确定性处理。
- 报告逐条包含标题、发布时间、来源链接、核心内容、附件链接；核心内容可回指原文快照，数字、日期、编号可回查。
- 重复内容被合并，跨站转载不重复展示；订阅任务连续触发时只输出新增内容，零新增也生成说明报告。
- Trace 记录真实请求 URL、时间戳、响应哈希和 Agent 决策，可向评委证明未硬编码。
- 操作文档能让干净环境按步骤安装、运行、验证；Demo 视频覆盖输入到输出的完整流程和多个问题。

## 4. 开发工作流

每个阶段都按同一套闭环执行：

1. 明确阶段目标和验收口径。
2. 写最小测试或评测样例，先定义“什么叫通过”。
3. 实现最少代码，优先打通主路径，再补失败路径。
4. 运行单元测试、模块评测或端到端验证。
5. 生成对应教学文档，解释新增代码的职责、关键数据结构、技术取舍和验证方法。
6. 更新阶段状态与下一阶段风险。

代码层面的约束：

- 不做未被要求的功能，不提前抽象。
- 只改当前阶段必要文件，不顺手重构相邻代码。
- 能用确定性代码解决的部分不依赖 LLM。
- 所有抓取、筛选、去重、增量逻辑都必须能通过测试或 trace 证明。

## 5. 技术路线基线

系统形态：FastAPI 后端 + React Web UI + CLI。后端用 SQLite WAL 做持久化，APScheduler 做定时任务，httpx 与 Playwright 做双引擎采集，selectolax/lxml/trafilatura 做解析清洗，python-docx 生成报告。前端通过 SSE 展示 Agent 时间线、意图确认卡、漏斗统计、订阅面板和报告下载。

核心思想沿用原架构文档：

- BidQL：自然语言编译后的中间表示，保存主题、地区、时间 AST、调度计划和置信度。
- TenderGraph：带检查点的状态图编排器，每个 Agent 是节点，校验失败通过条件边进入修复回路。
- Adapter-as-Code：LLM 只在离线编译适配器时参与，候选选择器必须经过夹具验证后冻结，运行期确定性执行。
- Session Vault：人工登录、加密持久化、健康探针、失效降级提示。
- Evidence Gate：字段和摘要句都带证据 span，事实不确定宁可不写。
- Cluster Ledger：运行内去重与跨运行推送去重分开，订阅账本以项目簇键为幂等主键。

## 6. 阶段计划与验收表现

| 阶段 | 目标 | 主要产出 | 验收表现 | 教学文档 |
|---|---|---|---|---|
| P0 工程骨架 | 建立可复现的项目底座 | pyproject、目录结构、配置、数据库初始化、健康检查 | 三条命令可启动健康检查；配置错误启动即失败；SQLite WAL 生效 | 00_工程骨架与配置管理.docx |
| P1 编排器与事件流 | 把流程做成可回放的状态图 | TenderGraph、RunState、Checkpointer、EventBus、Trace 表 | 空跑图输出 node_started/tool_called/node_finished；中断后可从检查点恢复 | 01_TenderGraph状态图编排器.docx |
| P2 意图编译器 | 自然语言转 BidQL | 时间文法、行政区划、调度解析、LLM 通道接口、仲裁器 | 赛题四例解析正确；相对/绝对时间在不同触发日期下求值正确 | 02_BidQL意图解析与时间AST.docx |
| P3 首个公开源闭环 | 先形成最小端到端能力 | 中国政府采购网适配器、源规划、最小 Word 报告 | 输入一个问题后抓取真实 URL，产出命名合规且五要素齐备的 Word | 03_HTTP采集适配器与首份报告.docx |
| P4 第二公开源与适配器注册 | 满足多来源基础要求 | 省级/全国公共资源交易平台适配器、注册中心、夹具验证 | 报告出现两个来源；适配器夹具验证失败时禁止上线 | 04_Adapter-as-Code与多源规划.docx |
| P5 登录站与会话金库 | 满足至少一个登录来源 | Playwright 登录命令、storage_state 加密保存、健康探针、登录源适配器 | 重启后登录态复用；会话失效时单源降级且其他源继续 | 05_登录态管理与浏览器采集.docx |
| P6 清洗与结构化抽取 | 从网页变成干净公告对象 | 噪声清洗、正文密度兜底、字段抽取链、附件提取、字段 span | 20+ 详情页夹具字段准确率达标；清洗后正文无关键事实丢失 | 06_网页清洗与字段抽取.docx |
| P7 去重与实体归并 | 解决转载和同项目多来源 | URL 规范化、SimHash64、标题相似度、业务键 Union-Find | 去重金标集 P/R 达标；报告中同项目多来源合并展示 | 07_SimHash与项目簇归并.docx |
| P8 证据链与事实闸门 | 让核心内容可验证 | 快照存证、硬约束校验、数字/日期/编号回查、证据回看接口 | 随机抽检 10 条可从报告回到快照证据；事实回查目标 100% | 08_证据链与事实一致性校验.docx |
| P9 摘要与报告引擎强化 | 生成可读且合规的 Word | 抽取式摘要、可选受限润色、报告模板、文件命名边界处理 | 含问号、超长、同分钟重复问题都生成合法文件名；零附件时表达清晰 | 09_摘要生成与Word报告引擎.docx |
| P10 调度与增量账本 | 实现 immediate/once_at/recurring | APScheduler、订阅表、push_ledger、水位线、手动触发 | 同一订阅连跑两次，第二份报告只含新增或显示零新增；账本零重复 | 10_定时任务与增量推送账本.docx |
| P11 Web UI 与 SSE | 让完整流程可演示 | React 输入页、意图确认卡、Agent 时间线、漏斗、订阅面板、下载入口 | 90 秒内能展示输入、解析、抓取、报告下载和订阅状态 | 11_前端交互与实时事件流.docx |
| P12 修复 Agent 与评测 Harness | 提升稳定性与反硬编码说服力 | 修复策略表、cassette 录制回放、eval all、指标报告 | 冷门主题触发修复回路；断网回放能跑；trace 可展示真实请求证据 | 12_自校正循环与评测体系.docx |
| P13 集成交付 | 收口四类交付物 | 详设文档、操作文档、Demo 脚本、最终回归、视频素材清单 | 赛题四类问题全部跑通；操作文档能在干净环境复现；交付物齐备 | 13_端到端联调与交付复盘.docx |

## 7. 教学文档机制

后续每写完一个阶段，我会额外生成一个教学 Word，结构固定：

- 本阶段要解决的问题：先讲业务目标，再讲技术目标。
- 源码地图：列出本阶段新增或修改的文件，每个文件一句话职责。
- 核心代码逐段解析：按函数或类拆解，说明输入、输出、关键状态变化和边界条件。
- 技术取舍：解释为什么这样写，以及为什么没有采用更复杂的方案。
- 验证方法：列出命令、测试数据、预期输出和常见失败现象。
- 扩展思考：给你补充相关工程知识，例如 AST、状态机、SimHash、Playwright 登录态、SSE、APScheduler 幂等。

教学文档不替代代码注释。代码注释只解释难读或关键约束；教学文档负责把代码讲透。

## 8. 测试与评测安排

| 类型 | 覆盖内容 | 通过标准 |
|---|---|---|
| 单元测试 | 时间解析、地区识别、文件命名、URL 规范化、SimHash、账本幂等 | 关键纯函数全覆盖，边界样例明确 |
| 夹具测试 | 列表页、详情页、附件链接、登录站样本脱敏回放 | 字段级准确率达标，选择器漂移能被发现 |
| 模块评测 | intent、extract、dedup、faithfulness、incremental | 输出结构化指标，失败样例可定位 |
| 端到端测试 | 赛题四类问题、随机省份主题、订阅二次触发 | 能生成真实报告，增量不重复 |
| 演示验证 | Web UI、CLI、断网 cassette、trace 证据 | 评委能看见完整链路和非硬编码证据 |

## 9. 计划文件结构

开发完成后的目录按以下方向组织：

```text
tendertrace/
  runtime/        # TenderGraph、状态、检查点、事件流、trace
  agents/         # intent、source_planner、collector、extract_clean、dedup、verify、repair、report、subscriber
  adapters/       # registry、compiler、contracts、http/browser executors
  intent/         # grammar、region、resolver、arbiter、llm_channel
  pipeline/       # cleaning、simhash、cluster、faithfulness、summarize
  vault/          # session_vault、probes
  scheduling/     # jobs、ledger、watermark
  report/         # docx_writer、naming
  evals/          # goldsets、fixtures、cassettes、harness
  app/            # FastAPI、SSE、静态前端
web/              # React 源码
docs/
  design/         # 详设文档
  operation/      # 操作文档
  teaching/       # 阶段教学 Word
outputs/          # 工具生成的报告
snapshots/        # HTML/截图证据
secrets/          # 加密登录态，本地不入仓
data/             # SQLite 数据库
```

## 10. 立即下一步

下一轮正式开工建议从 P0 开始，目标不是写很多代码，而是在最短路径内得到一个可启动、可建表、可健康检查、可跑测试的工程底座。P0 完成后立刻进入 P1，把事件流和状态图跑通；P2 后就可以解析赛题输入；P3 形成第一份真实 Word 报告，项目进入可演示状态。

开工前最好确认三项，但它们不阻塞 P0 到 P3：

- 是否需要真实邮件发送，还是本地 outbox + UI 下载即可满足初版。
- 登录站优先使用哪个免费会员账号或站点。
- 是否提供 OpenAI 兼容 LLM Key；如果没有，我会先按离线规则通道开发。
