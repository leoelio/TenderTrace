TenderTrace code-only public repository.

This repository contains only the technical runtime implementation files:
- Python package: tendertrace/
- Static web app: web/
- Build metadata: pyproject.toml
- Safe configuration template: .env.example

Runtime data, credentials, local environment files, tests with fake secret fixtures, generated reports, and delivery documents are intentionally excluded.
